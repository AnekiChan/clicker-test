using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Stats;
using System;
using Unity.VisualScripting;

public class UpgradeStat : MonoBehaviour
{
    // // public Action<int> OnPriceChanged;
    // [SerializeField] private Stat _stat;
    // private int _currentPrice = 0;
    // private float _multiplier = 5f;

    // void Start()
    // {
    //     CalculatePrice();
    // }

    // private OnEnable()
    // {
    //     CommonEvents.Instance.OnStatUpgraded +=
    // }
    
    // private void CalculatePrice()
    // {
    //     _currentPrice = Mathf.RoundToInt(Mathf.Exp(_stat.CurrentUpgradeLevel) * _multiplier);
    //     OnPriceChanged?.Invoke(_currentPrice);
    // }
    // public void Upgrade()
    // {
    //     if (CoinStats.CurrentCoins >= _currentPrice)
    //     {
    //         CoinStats.OnChangeStats?.Invoke(_stat, _currentUpgradeLevel, _currentPrice, _hasMaxUpgradeLevel && ((_maxUpgradeLevel - 1) <= _currentUpgradeLevel));
    //         CalculatePrice();
    //     }
    // }
}
